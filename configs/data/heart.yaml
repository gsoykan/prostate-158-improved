_target_: src.data.heart_datamodule.HeartDataModule
data_dir: ${paths.data_dir}
img_dir: heart/images
label_path: heart/golds
train_img_alias: tr
test_img_alias: ts
val_img_alias: val
batch_size: 32 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 0
pin_memory: False
